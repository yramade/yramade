#include <stdio.h>
#include <stdlib.h>
#include <time.h>

void main() {
  /* 한 줄에 하나의 정수형 변수 선언 */
  int Number;
  int Age;

  /* 같은 자료형은 콤마(,)로 구분하여 선언 */
  int Person1, Person2;

  /* 선언과 함께 초기화 */
  int Page = 10;

  /* 같은 데이터를 한 번에 초기화 */
  Person1 = Person2 = 100;

  return;
}